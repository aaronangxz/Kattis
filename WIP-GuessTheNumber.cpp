#include <iostream>

using namespace std;

int main(void)
{
    int n;
    string in;
    int tries = 0;

    while (tries != 10)
    {
        cout << "500" << endl;
        cin >> in;
        if (in == "lower")
        {
            /* code */
        }
        else if (in == "higher")
        {
            /* code */
        }
        else if (in == "correct")
        {
            /* code */
        }
        tries ++;
    }
    
} 