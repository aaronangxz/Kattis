#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main(void)
{
    int cases, trips;
    vector<string> names;
    scanf("%d",&cases);

    for (int i = 0; i < cases; i++)
    {
        scanf("%d",&trips);
        for (int j = 0; j < trips; i++)
        {
            scanf("%s",names[i]);
        }

        for (int k = 0; k < names.size(); i++)
        {
            string match = names[k];

            for (int l = 0; l < names.size(); l++)
            {
                if (names[l] == match)
                {
                    //To be continued
                }
                
            }
            
            
        }
        
        
    }
    

}