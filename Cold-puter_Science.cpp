#include <iostream>
#include <string>
#include <sstream>
#include <vector>

using namespace std;


int main(void)
{
    int temp, count;
    string num;

	getline(cin, num);
    stringstream input_num (num);

    vector<int> input;

    for (int i = 0; i < count; i++)
    {
        /* code */
    }
    
    //To be continued

}